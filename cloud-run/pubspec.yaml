name: cloud_provision_server
publish_to: none # not intended for publication to pub.dev

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  shelf: ^1.2.0
  shelf_router: ^1.0.0
  shelf_static: ^1.0.0
  dotenv: ^4.0.1

dev_dependencies:
  http: ^0.13.0
  uuid: 3.0.6
  googleapis_auth: ^1.3.1
  googleapis: ^9.2.0
  jwt_decoder: ^2.0.1
  shelf_cors_headers: ^0.1.3